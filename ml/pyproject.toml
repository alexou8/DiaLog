[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dialog-ml"
version = "0.1.0"
description = "DiaLog offline research / model-development pipeline (not deployed)."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]
